#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    for(char i=97;i<=122;++i)
    {
        putchar(i);
    }
    for(char i=65;i<=90;++i)
    {
        putchar(i);
    }
}
